import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(tnp());
}

MaterialApp tnp(){
  return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: const Text('Training and Placement'),),
      )
  );
}